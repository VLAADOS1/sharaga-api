# Agent Rules For This Workspace

## Java/Spring code style

- Do not write manual getters/setters in entities and DTO-like classes.
- Use Lombok annotations instead (`@Getter`, `@Setter`, `@NoArgsConstructor`, etc.).
- Use `@Autowired` for dependency injection in Spring components.
